####
# <%= scope.lookupvar('username') %>.yaml 
#
# /!\ DO NOT EDIT: this file is managed by Puppet
#     All local modification will be lost at the next puppet update
#
# YAML description of a real user associated to the local account '<%= scope.lookupvar('sysadmin::login') %>' 
#

<%= scope.lookupvar('username') %>:
  :firstname: <%= scope.lookupvar('firstname') %>
  :name: <%= scope.lookupvar('name') %>
  :email: <%= scope.lookupvar('email') %>
  :sshkeys: 
  - <% sshkeys.each do |key, val| %> :<%= key %>: <%= val %> 
    <% end -%>

######################
# eof
#
# Local Variables:
# mode: yaml
# End:
